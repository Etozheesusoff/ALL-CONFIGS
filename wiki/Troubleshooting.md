# **🛠️ Troubleshooting**

**Этот документ дублирует и расширяет раздел `docs/troubleshooting/`, но адаптирован под формат Wiki.**

---

## **Конфиг не загружается**

Проверьте:
1. Путь к файлу.
2. Права доступа (Windows: не read-only, нет блокировки антивирусом).
3. Правильность параметров запуска (например, `+exec autoexec.cfg` для CS2).

---

## **Резкое падение FPS**

- Закройте все фоновые программы.
- Проверьте температуру CPU/GPU.
- Убедитесь, что в панели управления GPU:
  - отключен V-Sync;
  - включен режим максимальной производительности.

---

## **Вылеты после изменения конфигов**

- Верните резервную копию.
- Удалите изменённые файлы, чтобы игра создала дефолтные.
- Применяйте конфиги по одному (сначала `cs2_video.txt`, потом `autoexec.cfg`).

---

## **Когда писать в Issues**

Если:
- проблема повторяется только с нашими конфигами;
- вы можете описать шаги воспроизведения;
- у вас есть логи, скриншоты или dump-файлы.

Используйте:
- шаблон `Bug Report` для багов,
- шаблон `Feature Request` для пожеланий.
