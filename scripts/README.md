# **🔧 Scripts / Скрипты установки**

**В этой папке находятся вспомогательные скрипты для установки и обновления конфигов.**

## **📂 Файлы**

* `install.py` — основной кроссплатформенный инсталлер (Python);
* `install.sh` — оболочка для Linux/macOS, вызывает `install.py`;
* `install.bat` — оболочка для Windows, вызывает `install.py`.

---

## **▶ Рекомендуемый способ**

### **Windows**

```bat
install.bat
```
или двойной клик по `install.bat`.


### **Linux / macOS**
```
chmod +x install.sh
./install.sh
```

## **🔁 Типичный сценарий**

* Клонировать репозиторий: 
```
git clone https://github.com/Etozheesusoff/ALL-CONFIGS.git
cd ALL-CONFIGS
```

* Запустить скрипт установки (в зависимости от ОС);
* Следовать инструкциям в консоли (выбор игры, GPU, уровня железа);
* Перезапустить игру.

## **⚠ Важно**

**Скрипты не должны изменять системные файлы вне папок игр.**
**Перед установкой рекомендуется закрыть запущенные игры и лаунчеры.**


***

## `scripts/install.sh`

```bash
#!/usr/bin/env bash
# ALL-CONFIGS install.sh
# Simple wrapper around install.py

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="${SCRIPT_DIR}/.."

cd "$ROOT_DIR"

if ! command -v python3 >/dev/null 2>&1; then
  echo "python3 is not installed. Please install Python 3 and try again."
  exit 1
fi

echo "=== ALL-CONFIGS Installer (Linux/macOS) ==="
python3 "scripts/install.py"
```
