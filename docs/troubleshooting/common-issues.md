
***

## `docs/troubleshooting/common-issues.md`


```markdown

# **🛠️ Common Issues / Частые проблемы**


**Этот документ помогает решить самые распространённые проблемы при использовании конфигов ALL-CONFIGS.**


---


## **1. Конфиг не применяется**

**Симптомы:**

* Настройки в игре не изменились;
* Команды из `autoexec.cfg` не работают.


**Проверка:**

1. Убедитесь, что файл лежит в правильной папке (см. `CS2/README.md` или `docs/guides/installation/\*.md`).

2. В CS2:

	* добавьте в параметры запуска: `+exec autoexec.cfg`


3. Проверьте кодировку:

	* Файл должен быть в **UTF-8 без BOM** или ANSI.


---


## **2. FPS ниже, чем ожидалось**


**Возможные причины:**

	* Включен V-Sync;
	* Систему ограничивает процессор (CPU bound);
	* Фоновые процессы нагружают систему.


**Решения:**

	* Отключите V-Sync (в игре и в панели GPU);
	* Закройте лишние приложения (браузер, Discord overlay и т.п.);
	* Проверьте температуру CPU/GPU (перегрев → троттлинг).


---


## **3. Вылеты игры после применения конфига**

**Причины:**

	* Неверные или устаревшие команды в `autoexec.cfg`;
	* Повреждённый `cs2_video.txt`.

**Решения:**

	1. Удалите или переименуйте `autoexec.cfg` и `video.txt`.
	2. Запустите игру — она создаст дефолтные файлы.
	3. Примените конфиг заново, внимательно следуя инструкции.
	4. При необходимости используйте резервную копию, если вы её сделали.


---


## **4. Мышь ощущается «тяжёлой» или «плавающей»**

**Проверьте:**

	* Отключена ли акселерация мыши в ОС;
	* Используете ли вы стандартный `raw input` в игре (если доступно).

**Советы:**

	* Не меняйте чувствительность слишком резко;
	* Не включайте дополнительные фильтры/сглаживание движения мыши.


---


## **5. Проблемы со скриптом установки**

Если вы используете `scripts/install.py` / `install.sh` / `install.bat`:

	1. Запустите с правами администратора (Windows) или через `sudo` (Linux, только если нужно);
	2. Убедитесь, что путь к игре указан верно;
	3. Проверьте вывод скрипта — там могут быть подсказки.


---


## **Дополнительно**

* Общие рекомендации по оптимизации: `docs/guides/optimization/best-practices.md`
* Углублённое руководство по CS2: `docs/guides/optimization/cs2-guide.md`




